# seiskang.kr 강의 교안 비밀번호 보안 취약점 수정 작업 기록

- 작업일: 2026-08-31
- 대상 사이트: https://seiskang.kr
- 저장소: GitHub `seiskang/train` (main 브랜치, Vercel 자동 배포)
- 관리자 계정: justice11419@naver.com

---

## 1. 발단 — 발견된 문제

`edu/aiu.html`, `edu/ai.html` 두 페이지는 비밀번호로 잠긴 것처럼 보였지만, 실제로는 다음과 같은 치명적인 문제가 있었다.

- 강의 교안의 실제 내용과 정답 비밀번호 문자열이 페이지의 정적 HTML/JS 소스에 그대로 포함되어 있었음.
- 즉 view-source, curl, 검색엔진 크롤러 등으로 비밀번호를 한 번도 입력하지 않고도 전체 내용과 비밀번호 값을 그대로 열람 가능했음.
- 기존 노출된 공용 비밀번호 값: `youn`

사용자 확인 후 결정된 방침:

1. **"진짜 서버 인증으로 전환"** — 클라이언트 사이드에서 비교하는 방식이 아니라, 서버가 비밀번호를 검증하고 그 결과로만 콘텐츠를 내려주는 구조로 전환.
2. **비밀번호 교체(권장)** — 이미 노출된 `youn` 대신 새 비밀번호로 교체.
3. 새 비밀번호는 사용자가 직접 지정: **`html`** (관리 편의상 이후 발견된 모든 교안 페이지에 동일하게 적용).

---

## 2. 새로운 인증 구조 (아키텍처)

### 2.1 데이터 저장
- Postgres에 새 테이블 `lesson_pages` 추가.
  - `page` (TEXT, PRIMARY KEY) — 페이지 식별자 (`ai`, `aiu`, `ais`)
  - `password_hash` (TEXT) — bcrypt 해시
  - `content_html` (TEXT) — 잠금 해제 후 노출되는 실제 교안 HTML
  - `updated_at` (TIMESTAMPTZ)
- **중요**: 이 데이터는 오직 DB에만 존재하며, git 저장소(코드)에는 절대 커밋되지 않음.
  - 이유: Vercel 배포 설정상 저장소 전체가 정적으로도 서빙되고 있어(`server/server.js` 같은 서버 소스도 URL로 직접 열람 가능한 것을 확인함), 저장소에 커밋되는 것은 무엇이든 공개되는 것과 같기 때문.

### 2.2 신규 API (server/server.js)
- `POST /api/lesson-auth`
  - `{ page, password }` 입력받아 `bcrypt.compareSync`로 검증
  - 성공 시 `{ page }`를 담은 JWT(180일 만료)를 서명하여 httpOnly 쿠키(`lesson_<page>`)로 발급
- `GET /api/lesson-content?page=X`
  - 요청에 담긴 `lesson_<page>` 쿠키의 JWT를 검증
  - 유효하면 DB에 저장된 `content_html`을 `{ html }`로 반환, 아니면 401
- `POST /api/admin/lesson-content` (관리자 세션 필요 — `requireAdmin`)
  - `{ page, password, html }`을 받아 DB에 upsert
  - `password`를 생략하면 기존 해시를 유지한 채 콘텐츠만 갱신 가능

### 2.3 페이지(클라이언트) 구조
각 교안 페이지는 다음과 같은 "껍데기"만 정적으로 배포됨:
- 잠금 화면(비밀번호 입력 UI) — 정적 HTML/CSS
- 페이지 로드 시 `tryAutoUnlock()` 실행 → 유효한 인증 쿠키가 있으면 서버에서 콘텐츠를 받아 즉시 화면에 표시 (재방문 시 비밀번호 재입력 불필요, 최대 180일)
- 비밀번호 제출 시 `checkPassword()` (또는 페이지별 동일 로직) 실행:
  1. `POST /api/lesson-auth`로 비밀번호 검증 요청
  2. 성공 시 `GET /api/lesson-content`로 실제 콘텐츠를 받아와 `innerHTML`로 주입
  3. 실패 시 에러 메시지 표시, 콘텐츠는 절대 전송되지 않음

---

## 3. 실제 수정한 페이지 (총 3개)

### ① `edu/aiu.html` — "AI 시대, 명예훼손 예방교육"
- 기존 취약점: 잠금 화면 뒤에 전체 교안 + 실명 댓글 기능이 정적 소스에 그대로 존재.
- 조치: 서버 인증 구조로 전환, 교안 내용(약 14KB)을 DB로 이전.
- 댓글 기능(실명 댓글 작성/조회)은 콘텐츠 주입 후 `initComments()`를 재호출하도록 수정하여 그대로 유지.
- GitHub 커밋: `070d210` "Switch aiu.html lesson content to server-side auth"

### ② `edu/ai.html` — "생성형 AI로 자서전 쓰기 & 말벗 되기"
- 기존 취약점: 위와 동일한 구조의 문제.
- 조치: 서버 인증 구조로 전환, 교안 내용(약 6KB) DB 이전.
- GitHub 커밋: `4e84fec` "Switch ai.html lesson content to server-side auth"

### ③ `edu/ais.html` — "디지털 자서전과 내 삶의 노래 만들기" (추가로 발견된 취약점)
- 사용자 질문("열람 제한 비밀번호 팝업의 종류는?")을 계기로 저장소 전체를 재점검하다가 발견.
- 이 페이지는 ①②와는 **다른 방식**으로 구현되어 있었음: 화면 전체를 덮는 오버레이 팝업(`#ais-lock`) 뒤에 실제 교안 내용(약 20KB)이 그대로 정적 소스에 포함되어 있었고, 비밀번호 검증도 브라우저 JS에서 `if(입력값==='youn')` 형태로 하드코딩되어 있었음. 인증 상태는 `localStorage`로 기억.
- 즉 ①②와 동일한 근본 문제였고, 심지어 이미 노출되어 교체 대상이었던 옛 비밀번호(`youn`)를 그대로 사용하고 있었음.
- 조치:
  - `server.js`의 `LESSON_PAGES` 배열에 `'ais'` 추가 (커밋 `e423a5e` "Add ais lesson page to LESSON_PAGES")
  - 교안 내용을 DB로 이전, 페이지를 서버 인증 구조로 전환하되 기존 팝업의 시각 디자인은 그대로 유지
  - 완성목록 체크박스의 localStorage 기억 기능은 콘텐츠 주입 후 `initChecklist()`로 재호출하도록 유지
  - GitHub 커밋: `8960908` "Switch ais.html lesson content to server-side auth"

### 참고 — 비밀번호 보호가 없는 페이지들 (문제 아님, 확인만 함)
저장소 전수 조사 중 `edu/aik.html`, `자서전_워크북.html`, `인생 자서전 워크북.html`, `20260828.html` 등은 애초에 비밀번호 보호 자체가 없는 완전 공개 페이지로 확인됨 (별도 조치 불필요).

---

## 4. 검증 내역 (모든 페이지 공통)

각 페이지에 대해 다음을 실제로 확인함:

1. **정적 소스 검사** (GitHub raw 소스 직접 확인): 교안 실제 내용, 비밀번호 문자열(`html`, `youn` 모두) 이 페이지 소스 어디에도 없음.
2. **잘못된 비밀번호 거부**: 실제 UI 흐름으로 오답 입력 시 401 응답 및 에러 메시지 정상 표시 확인.
3. **정답 비밀번호(`html`) 정상 동작**: 원본과 동일한 전체 콘텐츠가 그대로 복원되어 표시됨 (SHA-256 해시로 원본과 바이트 단위 일치 확인).
4. **자동 로그인(쿠키) 유지**: 한 번 인증 후 재방문 시 비밀번호 재입력 없이 자동으로 콘텐츠 노출 확인 (최대 180일).
5. **기존 기능 유지 확인**:
   - aiu.html: 실명 댓글 목록 로드 정상
   - ais.html: 완성목록 체크박스 토글 및 localStorage 저장 정상, 인쇄 버튼 및 목차 이동 링크 정상

---

## 5. 남은 참고 사항 (조치하지 않음, 낮은 우선순위로 별도 판단 필요)

- **저장소 전체 정적 노출**: 이 사이트는 Vercel 설정상 `/api/*` 라우트 이외의 경로는 저장소 파일 전체가 그대로 정적 서빙됨. 예를 들어 `https://seiskang.kr/server/server.js`에 직접 접속하면 서버 소스 코드 원문이 그대로 노출됨.
  - 다만 비밀키·토큰 등은 전부 환경변수(`process.env`)로 관리되고 있어 코드 자체에 비밀값이 하드코딩되어 있지는 않음.
  - 라우팅 구조를 손대는 작업은 다른 공개 정적 페이지(예: `index.html`, `edu/` 폴더의 공개 페이지들)에 회귀(regression) 위험이 있어, 이번 승인 범위 밖으로 판단해 손대지 않음.
  - **필요 시 별도로 검토/조치 요망.**

---

## 6. 최종 요약

| 항목 | 내용 |
|---|---|
| 수정된 페이지 | `edu/aiu.html`, `edu/ai.html`, `edu/ais.html` (총 3개) |
| 공통 비밀번호 | `html` |
| 인증 방식 | 서버 측 bcrypt 검증 + httpOnly JWT 쿠키 (180일) |
| 콘텐츠 저장 위치 | Postgres `lesson_pages` 테이블 (git 저장소에는 없음) |
| 관련 GitHub 커밋 | `1a297d6`, `31c7a27`, `070d210`, `4e84fec`, `e423a5e`, `8960908` |
| 미해결 참고 사항 | 저장소 전체 정적 노출(서버 소스코드 원문 열람 가능) — 별도 검토 필요 |

이 문서는 2026-08-31 Cowork 세션에서 수행한 보안 수정 작업 전체를 백업 목적으로 정리한 기록입니다.
